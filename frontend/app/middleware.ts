// import { NextResponse } from "next/server";
// import { initSocket } from "./lib/socket";

// export function middleware(req: Request & { socket: { server: any } }) {
//   console.log("hieelo");
    
//   initSocket(req.socket.server);
//   return NextResponse.next();
// }
